import * as THREE from "three";
export default class Cone extends THREE.Mesh {
    constructor(color: string, coneRadius?: number, coneHeight?: number, radialSegments?: number, controlsOpacity?: number);
}
